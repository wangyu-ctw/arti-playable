# 两个仓共用的忽略规则（本文件两仓都入库）。
# 仓专属规则在 utils/repo/exclude-build（打包产物仓）/ exclude-origin（源码仓），由 utils/repo/setup.sh 装进各自的 info/exclude。
# 架构说明见 utils/repo/README.md。

# 依赖 / 系统文件
node_modules/
**/.DS_Store
*.log

# IDE / agent 本地配置与临时目录
.idea/
.vscode/
.tmp/
.agents/
.claude/
skills-lock.json

# 内部服务域名（本地配置，不入库）
utils/artifex.host

# 源码仓的 git 目录（与打包产物仓共用同一工作区）
.git-origin/
